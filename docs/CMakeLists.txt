cmake_minimum_required(VERSION 3.28)

project(scry_docs LANGUAGES NONE)

get_filename_component(SCRY_ROOT_DIR "${CMAKE_CURRENT_SOURCE_DIR}/.." ABSOLUTE)

# The release number lives once, in the root project() call; read it from there
# rather than from a header that is itself generated from it.
file(
  STRINGS "${SCRY_ROOT_DIR}/CMakeLists.txt"
  SCRY_DOCS_VERSION_LINE
  REGEX "^[ \t]*VERSION[ \t]+[0-9]+\\.[0-9]+\\.[0-9]+[ \t]*$"
  LIMIT_COUNT 1
)
if(NOT SCRY_DOCS_VERSION_LINE MATCHES "([0-9]+)\\.([0-9]+)\\.([0-9]+)")
  message(FATAL_ERROR "Could not determine the project version from CMakeLists.txt")
endif()
set(SCRY_DOCS_VERSION_MAJOR "${CMAKE_MATCH_1}")
set(SCRY_DOCS_VERSION_MINOR "${CMAKE_MATCH_2}")
set(SCRY_DOCS_VERSION_PATCH "${CMAKE_MATCH_3}")
set(
  SCRY_DOCS_VERSION
  "${SCRY_DOCS_VERSION_MAJOR}.${SCRY_DOCS_VERSION_MINOR}.${SCRY_DOCS_VERSION_PATCH}"
)

# <scry/version.hpp> is generated by the library build, which this
# documentation-only project never configures; generate a copy here so the
# public version surface still appears on the site.
set(SCRY_DOCS_GENERATED_DIR "${CMAKE_CURRENT_BINARY_DIR}/generated")
set(PROJECT_VERSION_MAJOR "${SCRY_DOCS_VERSION_MAJOR}")
set(PROJECT_VERSION_MINOR "${SCRY_DOCS_VERSION_MINOR}")
set(PROJECT_VERSION_PATCH "${SCRY_DOCS_VERSION_PATCH}")
configure_file(
  "${SCRY_ROOT_DIR}/cmake/version.hpp.in"
  "${SCRY_DOCS_GENERATED_DIR}/include/scry/version.hpp"
  @ONLY
)

# Ubuntu 24.04, the documentation CI host, provides Doxygen 1.9.8. Keeping
# that version as the floor makes local and hosted warning behavior agree.
find_package(Doxygen 1.9.8 REQUIRED COMPONENTS doxygen dot)

file(GLOB SCRY_DOCUMENTED_HEADERS CONFIGURE_DEPENDS "${SCRY_ROOT_DIR}/include/scry/*.hpp")
list(
  APPEND SCRY_DOCUMENTED_HEADERS
  "${SCRY_DOCS_GENERATED_DIR}/include/scry/version.hpp"
  "${SCRY_ROOT_DIR}/include/scry/detail/reflection_meta.hpp"
  "${SCRY_ROOT_DIR}/include/scry/detail/reflection_registration.hpp"
  "${SCRY_ROOT_DIR}/include/scry/detail/reflection_schema.hpp"
)
list(TRANSFORM SCRY_DOCUMENTED_HEADERS PREPEND "\"")
list(TRANSFORM SCRY_DOCUMENTED_HEADERS APPEND "\"")
list(JOIN SCRY_DOCUMENTED_HEADERS " " SCRY_DOXYGEN_INPUT)

configure_file(Doxyfile.in Doxyfile @ONLY)

add_custom_target(
  scry_docs
  COMMAND "${CMAKE_COMMAND}" -E remove_directory "${CMAKE_CURRENT_BINARY_DIR}/html"
  COMMAND "${DOXYGEN_EXECUTABLE}" "${CMAKE_CURRENT_BINARY_DIR}/Doxyfile"
  BYPRODUCTS "${CMAKE_CURRENT_BINARY_DIR}/html/index.html"
  WORKING_DIRECTORY "${SCRY_ROOT_DIR}"
  COMMENT "Building Scry API documentation"
  VERBATIM
)
